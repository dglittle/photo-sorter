<tbody>
	<% for(var i=0; i < albums.length; i++) {%>
		<tr>
			<td>
				<a class="selectAlbum" href="/albums/<%= albums[i].id %>">
					<span><%= albums[i].name %></span>
					<div class="imageContainer">
					</div>
				</a>
			</td>
		</tr>
	<% } %>
</tbody>
